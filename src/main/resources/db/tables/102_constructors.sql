CREATE TABLE CONSTRUCTORS
(

    CONSTRUCTOR_ID  NUMBER(19, 0) NOT NULL GENERATED BY DEFAULT AS IDENTITY,
    CONSTRUCTOR_REF VARCHAR(255),
    NAME            VARCHAR(255),
    NATIONALITY     VARCHAR(255),
    URL             VARCHAR(255),
    PRIMARY KEY (CONSTRUCTOR_ID)

);