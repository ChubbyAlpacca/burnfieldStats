CREATE TABLE CIRCUITS (
    CIRCUIT_ID NUMBER(19,0) GENERATED BY DEFAULT AS IDENTITY,
    CIRCUIT_REF VARCHAR(255),
    NAME VARCHAR(255),
    LOCATION VARCHAR(255),
    COUNTRY VARCHAR(255),
    LAT VARCHAR(255),
    LNG VARCHAR(255),
    ALT VARCHAR(255),
    URL VARCHAR(255)
)